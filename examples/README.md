# Examples

`Calculator/CalculatorServer.php` is a runnable demonstration Server. It deliberately enables
`AllowAnonymousAuthenticator` for local development; production Servers should install an explicit
authenticator instead.

The v0.1 Calculator Server example is added after the protocol and Tool runtime are available.
